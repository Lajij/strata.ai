# Graph execution state — strata.ai v1

Last DECIDE update: 2026-08-04 (Australia/Sydney) — WAITING (`release-revision` is published on `codex/strata-v1-release-candidate` after all staged gates passed. `release-preview` is the next node and requires separate authorization for a Preview-only deployment and candidate browser barrier; Production remains untouched)

decide-gates | in-progress | decision #3 assigns sole approval to the repository operator/owner until revised; decision #4 answered `replace`; #5–#8 are signed for v1; #1–#2 and #9–#10 remain pending for deferred tracks
consolidate | done | new env names are primary with legacy fallbacks across app/scripts; `server-only` guards admin client; migration files checksum-identical; `.vercel/project.json` and relative middleware import preserved; `git diff --check` and all script syntax checks pass
local-gate | done | decision-4 implementation passed the full 9-gate barrier in one captured run on 2026-08-01; lint/build/security/production-ready/documents/AI/law/live-auth/live-members all passed; benign fallback warning remains until the operator replaces the mislabeled local `SUPABASE_SECRET_KEY`
dependency-hardening | done | Next and eslint-config-next 16.3.0, Tailwind integration 4.3.3, and compatible PostCSS/sharp/js-yaml/Hono resolutions produce a zero-vulnerability production audit; the complete 9-gate barrier passed afterward in strict order, including both cleanup-controlled live gates
release-decisions | done | operator signed #5 verified fallback, #6 yes, #7 no, and #8 defer-track-e-post-v1 on 2026-08-04; live AI is fail-closed behind exact `STRATA_AI_RELEASE_MODE=live`, while absent/invalid values retain fallback even when OIDC exists
release-revision | done | approved branch `codex/strata-v1-release-candidate` was created from `73eaaaf`; 169 staged paths matched the manifest with zero forbidden/outside-allowlist paths and zero credential-shaped hits; cached diff, static AI, lint, TypeScript, and operator build gates passed; the release commit was pushed to `origin` with excluded artifacts left unstaged
release-preview | waiting(operator-preview-authorization) | requires a fresh Preview-only deployment from the published immutable branch, explicit `STRATA_AI_RELEASE_MODE=fallback`, Ready/deployment-id evidence, Production before/after isolation, and the approved candidate browser barrier; no deployment is authorized by the Git-only approval
preview-1 | done | fresh Preview `https://strata-mwll397eb-jjlecocq-8964s-projects.vercel.app` passed `verify:ai-browser` 10/10 after the persistence-status UI repair; the preceding corrected Preview passed recovery 6/6 and auth/role 16/16. The browser verifier performed no Vercel or Production mutation
auth-harden | done | migration `20260801082608_harden_member_lifecycle_audit` applied to `osgvagsouxgbrnbljhxb`; object read-back and both live auth/member gates passed with zero test residue
recovery-flow | done | corrected Preview passed all 6 observations: callback, update form, password update, local session clear, new-password sign-in, and independent cleanup
role-gate | done | corrected Preview passed all 16 auth/role observations: signed-out, admin, ordinary member, suspended member, hidden records, lifecycle states, self-lockout controls, and scoped cleanup
fe-freeze | done | backend/data/auth/API and acceptance-suite contracts frozen in `FRONTEND-CONTRACT.md`; local-only contract verifier, lint, build, and diff checks pass
fe-inventory | done | `FRONTEND-GAPS.md` records 20 gaps and 3 stop-and-review change requests against the frozen contract; decision-4 edge re-scoped to the `fe-journey:*` integration nodes (see "Wave 2 fe-inventory evidence" below)
fe-journey:login | done | G-L1 closed and the corrected Preview passed the full 16/16 auth/role browser regression gate
fe-journey:dashboard | done | G-D1 closed: `refreshData()` rebases client card state from the successful authoritative RLS payload and clears only a selected card no longer visible; dashboard/frontend-contract/lint/build/diff gates pass
fe-journey:projects | done | dedicated Projects destination renders every frozen project field, RLS-empty state, complete evidence references, and ProjectAiTool; focused projects/frontend/dashboard/drilldown/lint/build/diff gates pass, and fresh Preview AI proof includes `projectStatus: true`
fe-journey:decisions | done | G-C1–G-C8 closed: all five authenticated workflow writes, authoritative refresh, proposal/condition state, card audit history, and accessible status UI pass focused/static/type/lint/security/build gates; Preview browser proof remains part of the later fan-in
fe-journey:documents | done | G-DOC1/G-DOC3 closed: authenticated multipart upload preserves FormData boundaries, surfaces status, refreshes in place, and renders extraction state independently from optional file size; local source/type/lint/security/search/contract/workflow/build gates pass
fe-journey:admin | done | G-A1/CR-3 closed and verified: privileged member controls remain server-authorized, current-member role/status/access controls are disabled, and Settings exposes truthful read-only session/source information; focused admin/static/contract/lint/build gates pass and Preview auth proof remains 16/16
fe-qa | done | fresh-eyes review found and then cleared truthful-data, mobile sign-out/navigation/focus, modal lifecycle, short-viewport dialog, target-size, contrast, skip-link, no-op-control, and semantic-link defects; the final local browser QA passed 13/13 with both roles and no console/framework errors
intake:{thread} | blocked(decisions-1,2) | mailbox must not be read before scope/confidentiality answers
review-queue | blocked(intake:{thread},decision-3) | human approval required
register | blocked(review-queue) | human-reviewed records only
dash | done | dashboard aggregates, attention items, activity, and project insights expose source-record references; authenticated RLS results never substitute fallback rows; static and read-only live gates pass
drilldown | done | card, document, and project detail surfaces expose exact parent and nested source-record references; static, live read, lint, and build gates pass
search | done | RLS-payload-only search spans cards, documents, projects, activity, budgets, vendors, and members; every result carries source references; behavior, regression, live-read, lint, and build gates pass
meeting-mode | deferred(decision-7-no) | signed decision #7 excludes Track B/meeting-mode from v1; resumption requires a new scope decision plus the blocked register pipeline
eve-tools | done | `eve@0.29.4` + AI SDK 7 installed; `withEve()` integration and Node 24 contract compile; production fail-closed Supabase bearer auth passes verified identity into each run; four read-only tools revalidate active server-side member scope and return citations or explicit `Evidence missing`; two draft-only tools require the configured operator before the human prompt and revalidate that operator after resume; all unrelated Eve built-ins disabled; Eve build/info, static verifier, lint, security verifier, and integrated build pass
eve-evals | done | eight deterministic Eve HTTP/session evals pass 40/40 hard gates for scope, citations, evidence-missing fallback, cross-session isolation, operator approval, human denial, and unauthorized-member denial; fixture mode is explicit-env, non-production, and loopback-only
eve-drafts | done | decision #3 assigns sole approval to the repository operator/owner; proposal and approval-condition writes are idempotent status=draft upserts with audit evidence, never publish/send/activate; server-only `STRATA_EVE_APPROVER_USER_ID` is configured as a sensitive Preview-only variable and was included in the Ready Preview
e-contracts | deferred(decision-8) | signed decision #8 defers Track E post-v1
e-confirmation-model | blocked(e-contracts,auth-harden,decision-10) | deferred with Track E
e-storage-policy | blocked(decisions-8,9) | deferred with Track E; no live migration authorized
e-migration | blocked(e-confirmation-model) | deferred with Track E; destructive/live changes hard-stop
e-types | blocked(e-migration) | deferred with Track E
e-views | blocked(e-migration) | deferred with Track E
e-routes | blocked(e-types,e-views,e-verify) | deferred with Track E
e-seed | blocked(e-migration) | deferred with Track E; email-derived figures must remain unconfirmed
e-extractor-build | blocked(e-contracts,e-migration) | deferred with Track E
e-extractor-run | blocked(e-extractor-build,intake:{thread}) | deferred with Track E
e-verify | blocked(e-contracts,e-confirmation-model,e-storage-policy) | deferred with Track E
e-v0 | blocked(e-contracts) | deferred with Track E
e-wire | blocked(e-routes,e-v0,e-storage-policy,e-verify) | deferred with Track E
preview-n | done(operator-accepted-variance) | Preview `dpl_Dkaot5hqJd6u66QbX4uadQLDKz5S` is READY and correctly targeted `preview`; approver env is sensitive/Preview-only; the explicitly approved `verify:auth-browser` gate passed 16/16 with scoped cleanup. The operator explained that the new errored Production deployment came from clicking Deploy in the Vercel dashboard and explicitly authorized continuing; the Production-isolation failure remains recorded as a release variance
rehearsal | done(operator-signoff-to-proceed) | local Eve rehearsal passed 8/8 evals and 40/40 gates; fresh Preview `dpl_HQ7AD2sLp5tij1dyryzYPsJiiVFr` passed all 13 read-only UI observations with zero console errors and zero failed responses; Production listings were identical before/after. The operator instructed the graph to move to the next phase on 2026-08-03
go-no-go | waiting(operator-final-decision) | `GO-NO-GO.md` remains NO-GO/HOLD: release decisions and dependency security are resolved, but the release tree is not committed/reviewable, the hardened tree has not been deployed/verified as an exact Preview candidate, and no Ready Production rollback target exists. Production remains untouched

## Routers

- decision #1: pending — Track B blocked; mailbox unread
- decision #2: pending — Track B blocked; mailbox unread
- decision #3: **answered 2026-08-02** — repository operator/owner solely approves/corrects/holds/excludes imported records and approves Eve drafts until the policy is revised
- decision #4: **answered `replace` on 2026-08-01** — delivered cards/votes/updates/people vocabulary is canonical; Projects and Budget are new destinations in that design language. The decision-4 edges were removed from all six `fe-journey:*` rows; their independent role/login/journey dependencies remain explicit
- decision #5: **answered `verified fallback mode` on 2026-08-04** — live Gateway remains explicit opt-in
- decision #6: **answered `yes` on 2026-08-04** — current Preview names and server-only boundary approved
- decision #7: **answered `no` on 2026-08-04** — Track B/meeting-mode excluded from v1 fan-in
- decision #8: **answered `defer-track-e-post-v1` on 2026-08-04** — Track E excluded from v1 fan-in
- decision #9: pending — Track E storage policy blocked
- decision #10: pending — Track E confirmation model blocked

## Open worktrees

- `/Users/jjlecocq/Documents/Codex/strata.ai` | branch `main` | shared dirty working tree; no journey worktrees opened

## Hard stops hit

- `release-revision` GitHub authentication prerequisite (2026-08-04): `gh` 2.95.0 is installed and `origin` is `https://github.com/Lajij/strata.ai.git`, but `gh auth status` reports the active `Lajij` token is invalid. Per the publish workflow, no branch, stage, commit, push, PR, or deployment action was attempted. Resume after `gh auth login -h github.com` and a passing `gh auth status`.
- GitHub authentication resolution (2026-08-04): the operator completed the browser/device flow; `gh auth status` outside the restricted sandbox then confirmed active keyring authentication for `Lajij` with HTTPS Git operations. The approved release branch was created afterward. No credential value was recorded in repository evidence.
- Release revision publication (2026-08-04): the manifest allowlist produced 169 staged path entries; automated classification found zero outside-allowlist and zero forbidden paths, and the staged-blob scan found zero credential-shaped values. `git diff --cached --check`, static `verify:ai`, lint, and `tsc --noEmit` exited 0; the operator-run integrated build had already compiled Next 16.3.0 and generated 9/9 pages. The approved commit was pushed on `codex/strata-v1-release-candidate`; excluded local/reference artifacts remain unstaged. No PR or deployment was created.
- `release-revision` integrated-build attempt cap (2026-08-04): the sandboxed `npm run build` completed Eve then Turbopack failed before Next compilation with `creating new process → binding to a port → Operation not permitted (os error 1)`. The approved elevated rerun produced the same exact failure. Per the two-consecutive-failures rule, no third build or alternate bundler attempt was made. Static `verify:ai`, lint, and `tsc --noEmit` all exited 0; no deployment, environment mutation, Supabase/AI request, commit, push, or Production action occurred.
- Operator build resolution (2026-08-04): the operator reran `npm run build` in the local terminal. Captured output shows Eve server build success, Next.js 16.3.0 compiled successfully in 2.5s, TypeScript completed in 2.6s, page data completed, 9/9 static pages generated, and the route table retained `ƒ /recover` plus `ƒ Proxy (Middleware)`. This resolves the Codex-runtime-only build blocker; it does not authorize Git or deployment mutations.
- `fe-journey:decisions` local-build authorization (2026-08-03): two consecutive elevated `npm run build` requests timed out in the automatic approval review before process creation. Per the node's two-attempt hard stop, no third attempt was made. No build, deployment, or remote mutation occurred; `npx tsc --noEmit` subsequently exited 0.
- `local-gate` attempt cap reached: two elevated executions were rejected at the authorization boundary for live Supabase mutations. Track stopped pending explicit operator risk approval; no workaround attempted and no remote state changed by those rejected runs.
- Goal blocked audit satisfied: the same operator-approval condition remained unresolved for three consecutive goal turns, and every remaining node is edge-blocked by `local-gate` or a pending human router.
- Follow-up diagnosis router contradiction (2026-08-01): the sanitized read-only Node diagnostic, using the verifier's `.env.local` → `.env` loader and key precedence, captured `resolvedProjectRef: "osgvagsouxgbrnbljhxb"`, `selectedServiceVariable: "SUPABASE_SECRET_KEY"`, `listBuckets: { data: [], error: null }`, and `getBucket: { data: null, error: { name: "StorageApiError", statusCode: "404", message: "Bucket not found" } }`.
- SQL cross-check evidence (2026-08-01): Supabase `execute_sql` on project `osgvagsouxgbrnbljhxb` with `select id, public, file_size_limit from storage.buckets order by id;` returned `[{"id":"strata-documents","public":false,"file_size_limit":52428800}]`.
- Root-cause status and branch: env misresolution and physical bucket absence are disproven by the captured ref and SQL row, but the Storage API did not list the existing bucket. This contradicts all three deterministic router branches, so the hard-stop `WAITING` branch was taken. No migration or other remote mutation was applied.
- Barrier result evidence: `npm run lint` exited 0; the elevated `npm run build` exited 0 after compiling, TypeScript checking, and generating 8/8 static pages; `npm run verify:security` exited 0 with `RLS and AI context verification passed.`; `npm run verify:production-ready` exited 1 with exact error `document bucket lookup: Bucket not found`. Result: 3/9 passed, gate 4 failed, gates 5–9 not run.

## Resolutions (2026-08-01, second follow-up)

- Storage contradiction root cause: `SUPABASE_SECRET_KEY` in `.env.local` holds a **publishable-format** key (`sb_publishable_…`), not a secret key. A privilege probe (no values printed) showed it fails Auth admin and lists zero buckets (anon-level RLS filtering), while `SUPABASE_SERVICE_ROLE_KEY` is a genuine `service_role` JWT that lists `strata-documents`. The bucket always existed; no migration was needed and none was applied.
- Fix applied: format-aware resolver `scripts/service-key.mjs` (accepts only `sb_secret_…` or `service_role` JWT; warns and falls back to `SUPABASE_SERVICE_ROLE_KEY` on mislabeled values) wired into all 15 scripts and mirrored in `src/lib/supabase/admin.ts`. Behaviour with neither key set is unchanged (fallback build unaffected).
- Second barrier failure (gate 6, `verify:ai`) root cause: the v0 frontend had replaced the AI assistant with a client-side keyword matcher (`assistant-engine.ts`); no component called `/api/ai/[task]`, rendered citations, or mounted document/project/budget AI tools. Same regression class previously recorded for signed-out locking.
- Fix applied: recovered the AI UI contract from git HEAD into `src/components/assistant/ai-tools.tsx` (runAiTask with verification-marker support, AiResultCard with citations + mode/model badge + disclaimer, DocumentAiTool, ProjectAiTool, BudgetAiTool); mounted on documents-page (per-document Q&A), dashboard-page (budget insights + per-project status, via new `rawProjects` in app-store); wired building-assistant with a deterministic router (law/budget questions → cited server AI, other questions → local heuristics, API failure → local fallback preserved). `verify-ai-layer.mjs` now inspects the composed client surface (7 files); assertions unchanged.
- Barrier result: **9/9 passed in one run** — lint, build, verify:security, verify:production-ready (`storageBucket: true`), verify:documents, verify:ai, verify:law, live auth-flow, live member-management. Resolver fallback warning appeared on every live gate as designed.

## Preview-1 evidence (2026-08-01)

- Vercel link: `.vercel/project.json` remained unchanged with project `strata-ai`, project ID `prj_vV0YWNGeNd7ww61Z1eHnKnpRYFO0`, and org ID `team_yGfSSn3ynBOeR2WRcs2Z3lVi`; `vercel whoami` returned `jjlecocq-8964`, and `vercel teams ls` returned `jjlecocq-8964s-projects`.
- Preview environment names: authenticated `vercel env list preview --format json` returned exactly `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`, and `SUPABASE_SERVICE_ROLE_KEY`, each with target `["preview"]` and type `sensitive`. Values were supplied from `.env.local` through stdin and were never printed or edited.
- Server-key boundary: `src/lib/supabase/admin.ts` still begins with `import "server-only";`; `scripts/verify-production-ready.mjs` retains negative assertions against `SUPABASE_SECRET_KEY` and `SUPABASE_SERVICE_ROLE_KEY` in browser/app code; `middleware.ts:2` remains `import { updateSession } from "./src/lib/supabase/middleware";`.
- Fresh Preview: `vercel deploy --target preview --yes --format json` returned deployment ID `dpl_FhrPm7MHTK72ie5F1z55BuPAWzYc`, URL `https://strata-532rygzcm-jjlecocq-8964s-projects.vercel.app`, `readyState: "READY"`; authenticated `vercel inspect` independently returned `target: "preview"` and `readyState: "READY"`.
- Deployment protection: anonymous `curl` followed the Preview's `302` to Vercel login. The first share-link request using the alias hostname exited 1; read-only alias inspection showed no existing share URL; the corrected request using the resolved alias UID also exited 1. The explicit two-consecutive-failure hard stop fired. No protection setting, alias, production deployment, or source code was changed, and `verify:auth-browser` / `verify:ai-browser` were not run.
- Production before/after evidence: both authenticated `vercel list strata-ai --environment production --format json` captures contained the same single deployment — URL `strata-girsmzdpo-jjlecocq-8964s-projects.vercel.app`, state `ERROR`, target `production`, `createdAt: 1782599055388`; normalized comparison returned `productionUnchanged: true`.

## Wave 2 auth-harden evidence (2026-08-01)

- Branch scope: `auth-harden` was selected because it depends only on the completed `local-gate`; `recovery-flow` and `role-gate` remain Preview-dependent and were left to the separate Preview workstream.
- Server role matrix: `src/lib/member-authorization.ts` defines member-management capability as allowed for `admin`, `chair`, and `secretary`, and denied for `treasurer`, `member`, and `strata_manager`; both member mutation routes call `canManageMembers`. The existing members RLS policy independently uses the same positive role set.
- Lifecycle hardening: invite preparation now rejects existing active/suspended rows; route and migration guards reject activation without an Auth user, backwards active/suspended→invited transitions, authenticated identity/invitation-field rewrites, and direct self role/status/access changes.
- Audit guarantee: Supabase CLI created `supabase/migrations/20260801053901_harden_member_lifecycle_audit.sql`. Its invoker-security trigger inserts member lifecycle events into `public.audit_log` in the same transaction as the member write; the previous best-effort route inserts were removed to avoid duplicates.
- Static evidence: `npm run verify:security`, `npm run verify:auth-flow`, `npm run verify:member-management`, `npm run lint`, and `git diff --check` all exited 0. `npm run build` first failed only because sandboxed Turbopack could not bind an internal port; the authorized non-sandboxed rerun compiled, passed TypeScript, generated 8/8 pages, and exited 0.
- Approved migration application: local file SHA-256 `d8c65704014dba37a969766d47af8f08496516c18219dcaa7757f044d856a87e` was applied only to project `osgvagsouxgbrnbljhxb` as remote migration `20260801082608_harden_member_lifecycle_audit`; no other migration or project was changed.
- Remote read-back: `audit_member_lifecycle` and `enforce_member_lifecycle` both exist in `app_private`, are `SECURITY INVOKER`, have `search_path=""`, and deny direct `EXECUTE` to `anon` and `authenticated`. Both corresponding triggers exist on `public.members` and are enabled (`tgenabled = O`).
- Live gates: `STRATA_VERIFY_LIVE_AUTH=1 npm run verify:auth-flow` exited 0 with `Auth flow live verification passed.`; `STRATA_VERIFY_LIVE_MEMBERS=1 npm run verify:member-management` exited 0 with `Member management live verification passed.` The known mislabeled local `SUPABASE_SECRET_KEY` warning appeared and the approved resolver fallback was used.
- Transactional audit proof: read-back found `Created member`, `Updated member access`, and `Changed member status` events for the fixed member-management test target, all with `workflow = member-lifecycle`; function-source checks confirmed the activation, backwards-invite, self-change, and audit-insert guards.
- Cleanup proof: final SQL read-back returned `member_rows: 0` and `auth_users: 0` for `member-management-verify@example.com`. No Vercel command, production deployment, commit, or push was performed.

## Wave 2 recovery-flow evidence (2026-08-03)

- Node boundary: implementation proceeded locally while the separate Vercel workstream remains responsible for producing the fresh Preview artifact. The graph edge remains real because the node's output contract requires the recovery callback and password round trip to pass on that deployed URL.
- Current guidance: the Supabase Auth changelog and current password guide were checked before implementation. No recovery-specific SDK breaking change applies; the current documented flow remains `resetPasswordForEmail` with a configured redirect followed by authenticated `updateUser`. The implementation uses installed `@supabase/ssr@0.12.0` / `@supabase/supabase-js@2.108.2`, whose browser client is PKCE and detects recovery sessions in the URL.
- Public route: `src/app/recover/page.tsx` exposes `/recover`; the signed-out workspace links to it. The request stage derives only `new URL("/recover", window.location.origin)`, returns a generic eligibility message, and never accepts a caller-supplied redirect.
- Callback/update boundary: the client accepts Supabase's `PASSWORD_RECOVERY` event or a server-verified current user after a recovery callback, scrubs query/hash tokens with `history.replaceState`, requires matching passwords of at least 12 characters, calls `updateUser`, and signs the recovery session out locally. The normal active-member workspace gate remains unchanged and authoritative.
- Executable Preview gate: `verify:recovery-browser` creates a disposable active member, generates (but does not send) an admin recovery link, completes the UI password update, proves the new password signs in, and independently attempts audit/member/Auth cleanup with aggregate error reporting. Link and secret values are never logged. The command was added but deliberately not executed without a fresh Preview and live-verification authorization.
- Static evidence: `npm run verify:recovery` printed `Password recovery source verification passed`; `node --check scripts/verify-recovery-browser.mjs`, `npm run lint`, `npm run verify:security`, and `git diff --check` exited 0.
- Build evidence: the authorized `npm run build` exited 0. Eve/Nitro built; Next.js 16.2.9 compiled, passed TypeScript, generated 9/9 pages, listed `ƒ /recover`, and retained `ƒ Proxy (Middleware)`.
- Safety boundary: no recovery email was sent or read, no password or live Supabase row was changed, no Preview/Production deployment or configuration was mutated, and no secret, recovery link, commit, or push was emitted.
- First Preview attempt: Supabase replaced the requested fixed Preview redirect. The operator added the scoped Preview wildcard `https://*-jjlecocq-8964s-projects.vercel.app/**` to the Auth redirect allowlist; the next attempt reached the Preview callback, so redirect preservation is no longer the blocker.
- Second Preview attempt: `verify:recovery-browser` timed out waiting for `getByLabel('New password')`. Captured Supabase Auth logs showed the admin recovery-link request returned 200, `/verify` returned 303 to the Preview `/recover` route, the login method was `implicit`, no password-update request followed, and disposal cleanup deleted the test user successfully.
- Root cause: installed `@supabase/ssr@0.12.0` creates the normal browser client with `flowType: "pkce"`, while the admin-generated recovery link used by the mailbox-free verifier returned an implicit hash callback. Installed Auth client source rejects an implicit URL when initialized for PKCE, so the recovery session and `PASSWORD_RECOVERY` event never reached the form.
- Compatibility fix: `getSupabaseImplicitRecoveryClient()` uses only the public Supabase URL/key, keeps the session in memory (`persistSession: false`), disables refresh, and is selected only when `/recover` receives an implicit recovery hash. `RecoveryWorkspace` retains that exact client for `updateUser` after URL scrubbing. User-requested recovery emails and the rest of the app continue to use the existing SSR/PKCE client.
- Post-fix local evidence: `npm run verify:recovery`, `node --check scripts/verify-recovery-browser.mjs`, `npm run lint`, `npm run verify:security`, `git diff --check`, and the authorized integrated `npm run build` all exited 0. The build again generated 9/9 pages and listed `ƒ /recover`.
- Pre-fix deployment edge (resolved): Preview `https://strata-iknkxpypw-jjlecocq-8964s-projects.vercel.app` was exercised before the compatibility fix and remains stale. The corrected tree was subsequently deployed at `https://strata-5fe7lbisg-jjlecocq-8964s-projects.vercel.app`; only that corrected Preview may supply the pending recovery proof.
- Corrected-Preview attempt: Preview `https://strata-5fe7lbisg-jjlecocq-8964s-projects.vercel.app` reached `/recover` and rendered both password fields, proving the implicit-callback compatibility fix deployed successfully. The verifier then stopped before mutation because Playwright's non-exact `getByLabel("New password")` also matched `Confirm new password` and raised a strict-mode violation. The locator now uses `{ exact: true }`; no application behavior or recovery assertion changed. The subsequent rerun passed, as recorded in the next bullet.
- Corrected-Preview recovery rerun: `verify:recovery-browser` exited 0 with `ok: true`; all six observations were true (`callbackReached`, `updateFormShown`, `passwordUpdated`, `localSessionCleared`, `newPasswordSignsIn`, `cleanupComplete`). The known local service-key fallback warning appeared; no residue was reported.
- Corrected-Preview role gate: `verify:auth-browser` exited 0 with `ok: true`; all 16 observations were true, covering signed-out locking, admin/member capabilities, invite lifecycle, self-lockout controls, hidden records, suspension, and scoped cleanup.
- AI browser hard stop (resolved): the first attempt to start `verify:ai-browser` was rejected before process creation because the gate may export Preview-visible thread, document, budget, and project context to the configured AI destination. No AI query or remote mutation occurred in that rejected attempt. The operator subsequently approved the disclosure and the command ran, as recorded next.
- AI browser approval/run: the operator explicitly approved sending Preview-visible test context to the configured AI destination. The gate then ran and reached the first fallback card result with RLS-visible citations, but failed because `Saved to ai_outputs` was absent from the rendered page.
- Read-only diagnosis: remote `public.ai_outputs` contains all 20 expected base/observability columns; `authenticated` has INSERT/SELECT grants; member INSERT and visibility SELECT policies are present. Supabase API logs captured `POST /rest/v1/ai_outputs?select=id` status 201, `POST /rest/v1/audit_log` status 201, and the verifier's cleanup DELETE status 204. Persistence, audit, schema, grants, and RLS are therefore not the failure.
- AI UI root cause/fix: `AiResultCard` accepted `response.persisted` in its type but never rendered it after the frontend refactor. It now truthfully renders `Saved to ai_outputs` or `Not saved to ai_outputs`; `verify-ai-layer.mjs` statically guards both states. No AI request, prompt, citation, RLS, persistence, or browser assertion was weakened.
- Post-fix evidence: `node --check scripts/verify-ai-layer.mjs`, `npm run lint`, `git diff --check`, and the authorized integrated `npm run build` exited 0. Eve/Nitro built; Next.js 16.2.9 compiled, TypeScript passed, and 9/9 pages generated. This established the artifact subsequently deployed to the final Preview.
- Final Preview AI gate: `STRATA_BROWSER_URL=https://strata-mwll397eb-jjlecocq-8964s-projects.vercel.app npm run verify:ai-browser` returned `ok: true`; all 10 observations were true (`signedIn`, `hiddenAbsent`, `cardBrief`, `threadSummary`, `quoteRisk`, `lawLookup`, `documentQa`, `budgetInsight`, `projectStatus`, `persistedRows`). The known local service-key fallback warning remained benign.
- Final cleanup read-back: read-only SQL for marker `verify-ai-browser-2026-08-03T03:03:53.665Z` returned `ai_output_rows: 0` and `audit_rows: 7`. The seven temporary AI outputs were removed; the seven audit events remain as the expected generation audit trail.

## Wave 2 fe-journey:dashboard evidence (2026-08-03)

- G-D1 root cause: `AppStoreProvider` initialized `cards` from mapped `platformData.cards` once, while `StrataApp.refreshData()` replaced the parent `StrataAppData`. The refreshed RLS payload reached the provider, but its authoritative cards never rebased the independent client state.
- Rejected attempt: a synchronous `useEffect` reset passed the dashboard source verifier but failed ESLint's `react-hooks/set-state-in-effect` rule because it would add a cascading render. That implementation was removed rather than suppressing the rule.
- Final repair: the provider wraps `onDataRefresh`, awaits its successful `StrataAppData` result, maps that exact response, then replaces client cards and clears `selectedCardId` only when the refreshed RLS-visible set no longer contains it. Failed refreshes preserve the current UI; local mutations remain until the next successful authoritative refresh.
- Regression guard: `verify-dashboard-source.mjs` now requires the awaited refresh result, authoritative `setCards(refreshed.cards)` rebase, and stale-selection check. `FRONTEND-GAPS.md` records G-D1 closed without changing the frozen API or RLS contracts.
- Evidence: `node --check scripts/verify-dashboard-source.mjs`, `npm run verify:dashboard`, `npm run verify:frontend-contract`, `npm run lint`, `git diff --check`, and the authorized integrated `npm run build` all exited 0. The final build compiled, passed TypeScript, generated 9/9 pages, and retained `ƒ Proxy (Middleware)`.
- Safety boundary: no live verifier, Supabase mutation, migration, Vercel command/deploy, production action, credential edit/output, external message, commit, or push occurred for G-D1.

## Wave 2 fe-journey:projects evidence (2026-08-03)

- Node boundary: G-P1/G-P2 implementation already existed under decision #4 `replace`; this node verified that implementation rather than changing application behavior, schema, RLS, or AI prompts.
- Focused gate: new local-only `npm run verify:projects` checks the Projects desktop/mobile navigation and mount, RLS-empty disclosure, every frozen field (scope, status, progress, allowance, committed, invoiced, remaining, milestones, variations, AI summary), complete source/evidence references, project-status AI request/control, persistence-status presentation, and absence of direct fetch/Supabase/server-secret use in the page.
- Contract wiring: `FRONTEND-CONTRACT.md` documents the new projects journey gate and `verify-frontend-contract.mjs` requires it, preventing the verifier from disappearing independently of the frozen acceptance suite.
- Static evidence: `node --check scripts/verify-projects-source.mjs`, `npm run verify:projects`, `npm run verify:frontend-contract`, `npm run verify:dashboard`, `npm run verify:drilldown`, `npm run lint`, and `git diff --check` all exited 0.
- Browser evidence: the completed fresh-Preview `verify:ai-browser` result recorded `projectStatus: true`, proving Projects navigation, the `Refresh project AI` control, persisted output presentation, and the project-status task on the deployed tree.
- Build evidence: the authorized integrated `npm run build` exited 0; Eve/Nitro built, Next.js 16.2.9 compiled, TypeScript passed, 9/9 pages generated, and `ƒ Proxy (Middleware)` remained present.
- Safety boundary: no live script was rerun for this node; no Supabase/Vercel mutation, migration, production action, credential output/edit, external message, commit, or push occurred.

## Wave 2 fe-journey:admin evidence (2026-08-03)

- Node boundary: the People and Settings implementation already satisfied G-A1/CR-3 and the frozen role contract. This node added focused ownership evidence without changing application behavior, member assertions, server authorization, schema, RLS, or lifecycle rules.
- Focused gate: new local-only `npm run verify:admin` checks People/Settings navigation and mounts; the admin/chair/secretary UI role boundary; authenticated invite/update bindings; unauthorized and current-member control locks; accessible control/status names; authoritative refreshes; the exact six-role server capability matrix; independent authorization in both member routes; truthful read-only session/source presentation; and absence of mutation controls or server keys in Settings/People.
- Contract wiring: `FRONTEND-CONTRACT.md` documents the admin journey gate and `verify-frontend-contract.mjs` requires it. `FRONTEND-GAPS.md` now records G-A1/CR-3 as closed and verified while retaining the original inventory finding as audit history.
- Static evidence: `node --check scripts/verify-admin-source.mjs`, `npm run verify:admin`, static `npm run verify:auth-flow`, static `npm run verify:member-management`, `npm run verify:frontend-contract`, `npm run lint`, and `git diff --check` all exited 0. The auth/member commands explicitly reported static verification and did not enter their live modes.
- Browser evidence: the completed corrected-Preview `verify:auth-browser` result remains the deployed proof for this unchanged surface: all 16 observations were true, including pending invite creation, backwards-lifecycle rejection, self-lockout controls, ordinary-member invite/edit denial, suspension lockout, hidden-record filtering, and scoped cleanup.
- Build evidence: the authorized local `npm run build` exited 0; Eve/Nitro built, Next.js 16.2.9 compiled, TypeScript passed, 9/9 pages generated, and `ƒ Proxy (Middleware)` remained present.
- Safety boundary: no live verifier, Supabase/Vercel mutation, migration, deployment, production action, credential output/edit, external message, commit, or push occurred.

## Wave 2 fe-journey:decisions evidence (2026-08-03)

- Node boundary: the journey consumes only the frozen `/api/workflow/{action}` route and the existing RLS-filtered `StrataAppData` refresh path. No route authorization, schema, migration, RLS policy, AI prompt, document behavior, or release setting changed.
- Writable bindings: `CreateCardDialog` now posts `create-card`; the card drawer posts `add-message`, `create-proposal`, `cast-vote`, and `add-approval-condition`. Every request uses `authHeaders()`, surfaces server validation errors, disables its control while pending, and awaits the existing in-place authoritative refresh. The obsolete client-only `addCard`, `updateCard`, and `castVote` store APIs were removed.
- Workflow presentation: the create form exposes the frozen Card title/description/Create card names; the drawer exposes all eight remaining workflow names plus Proposal rationale and Vote note. The create-card vocabulary now matches the backend card model; votes use the route's fixed yes/no/abstain values instead of fabricated free-text options.
- Audit/status evidence: the adapter carries proposal title, approval conditions, messages, and per-card audit events into the composed card model. The drawer renders `Card audit history`, proposal state, conditions, and shared `StatusMessage` loading/success/error announcements.
- Static evidence: `npm run verify:workflow-ui` printed `Workflow UI source verification passed.`; `npx tsc --noEmit`, `npm run lint`, `git diff --check`, `npm run verify:frontend-contract`, `npm run verify:security`, `npm run verify:dashboard`, `npm run verify:drilldown`, `npm run verify:search`, and `npm run verify:admin` all exited 0.
- React/Next review: implementation follows the installed Next.js 16 client-component and route-handler boundaries; all mutations remain event-driven through authenticated POST handlers. The React best-practices review found no effect-derived state, unstable list key, global listener, or server-secret/client boundary regression.
- Build evidence: after explicit operator approval, `npm run build` exited 0. Eve/Nitro built; Next.js 16.2.9 compiled in 8.5s, TypeScript finished in 11.7s, 9/9 pages generated, the workflow route remained present, and `ƒ Proxy (Middleware)` remained active.
- Deferred browser proof: `verify:browser-workflow` was not executed. It creates and cleans live Preview workflow rows and remains operator-approval gated after a fresh Preview carries this tree.
- Safety boundary: no live verifier, Supabase/Vercel mutation, migration, deployment, production action, credential output/edit, external message, commit, or push occurred.

## Wave 2 fe-journey:documents evidence (2026-08-03)

- Node boundary: the journey binds the existing `/api/documents/create` contract and changes presentation mapping only. The route's active-member authorization, Storage bucket, document/attachment writes, failure cleanup, schema, RLS policies, extraction behavior, and document AI task remain unchanged.
- Multipart repair: `authHeaders()` now accepts an explicit JSON-or-multipart mode. JSON remains the default for all existing callers; multipart omits `Content-Type` so the browser supplies the required FormData boundary while retaining the session bearer token.
- Upload UI: Documents exposes labelled title, type, visibility, source-date, and file fields; accepts TXT/Markdown/PDF/DOCX; posts `new FormData(event.currentTarget)` to `/api/documents/create`; disables controls while pending; surfaces loading/success/error through `StatusMessage`; and awaits the existing authoritative in-place refresh.
- Extraction-state repair: `DocItem.extractionStatus` is independent from optional `fileSize`. Live `DocumentRecord.status` maps only to extraction status; fallback fixtures preserve their actual sizes and identify themselves as demo records. Document search indexes both fields independently.
- Focused gate: new local-only `npm run verify:documents-ui` checks multipart binding/header behavior, accessible controls, status states, refresh, supported formats, extraction/file-size separation, route authorization/storage/persistence/cleanup contracts, and absence of demo toast or server keys in the UI.
- Static evidence: `npm run verify:documents-ui`, `npx tsc --noEmit`, `npm run lint`, `npm run verify:security`, `npm run verify:search`, `npm run verify:frontend-contract`, `npm run verify:workflow-ui`, and `git diff --check` all exited 0.
- Build evidence: the authorized local `npm run build` exited 0. Eve/Nitro built; Next.js 16.2.9 compiled in 7.9s, TypeScript finished in 10.3s, 9/9 pages generated, `/api/documents/create` remained present, and `ƒ Proxy (Middleware)` remained active.
- Live-proof boundary: `npm run verify:documents` was not rerun because it performs cleanup-controlled live Storage/database mutations. Its existing full-barrier pass remains backend evidence; a fresh Preview UI proof belongs to the later browser fan-in.
- Safety boundary: no live verifier, document upload, Supabase/Vercel mutation, migration, deployment, production action, credential output/edit, external message, commit, or push occurred.

## Wave 2 fe-freeze evidence (2026-08-01)

- Scope/router boundary: `fe-freeze` consumed the completed `local-gate` and produced `FRONTEND-CONTRACT.md` without selecting either branch of pending decision #4 (replace the simplified frontend versus refactor into it). ~~`fe-inventory` is therefore blocked only on decision #4.~~ **Superseded 2026-08-01** — the second clause was a mis-scoped edge; see "Wave 2 fe-inventory evidence" below. The first clause stands.
- Frozen surface: the contract maps the current application views, `StrataAppData` shape, every member/document/workflow/finance/AI route and action, member role/state capabilities, security invariants, and the acceptance commands that protect them.
- Acceptance-suite hardening: `scripts/verify-frontend-contract.mjs` statically verifies the frozen contract and `package.json` exposes it as `npm run verify:frontend-contract`. The verifier is local-only and performs no network or remote mutations.
- Lifecycle consistency: `scripts/verify-auth-browser.mjs` now expects the hardened active→invited transition to be rejected with `Active or suspended members cannot be moved back to invited`, then continues through the valid suspension path. This aligns the browser acceptance contract with the completed `auth-harden` migration without weakening an assertion.
- Workflow source coverage: `scripts/verify-workflow-ui-source.mjs` now inspects the composed application files and requires bindings for all five `/api/workflow/*` actions. `npm run verify:workflow-ui` currently exits 1 at the first frozen gap, `Missing aria-label="Card title"`; `FRONTEND-CONTRACT.md` records this and the absent workflow bindings as implementation work for `fe-journey:decisions`, not as a passing claim.
- Static verification: `npm run verify:frontend-contract`, `npm run lint`, the authorized non-sandboxed `npm run build`, both edited-verifier `node --check` commands, and `git diff --check` exited 0. The build compiled with Next.js 16.2.9, passed TypeScript, and generated 8/8 static pages.
- Safety boundary: no live verification script, Supabase or Vercel command, migration, production mutation, credential edit, commit, or push was performed for this node.

## Wave 2 dash evidence (2026-08-01)

- Node boundary: `dash` consumed only the completed `local-gate` and seeded RLS-backed schema. It did not enter the downstream `drilldown` or `search` nodes and did not depend on Preview access or frontend decision #4.
- Traceability surface: `DashboardPage` now identifies its evidence boundary and exposes exact `card:<id>`, `audit_log:<id>`, and `project:<id>` references for aggregate metrics, attention items, the featured vote, recent activity, and project insight controls. Card references remain navigable through the existing card drawer.
- Source-ID preservation: `mapAudit` retains the Supabase `audit_log.id`; the building-platform adapter carries it as `sourceRef`. Local fallback activity uses explicit `fallback-activity:*` references so demo claims cannot be mistaken for database records.
- RLS/fallback correction: an authenticated Supabase response now returns exactly the rows visible through RLS, including truthful empty arrays. It no longer substitutes seeded fallback cards, documents, projects, activity, members, vendors, or budget lines when a live query returns zero rows; budget citation counts likewise describe only live records.
- Static gate: `npm run verify:dashboard` exited 0 with `Dashboard source-traceability verification passed.` It asserts visible source labels, navigable card references, audit/project references, absence of authenticated fallback substitution, and the live verifier's hidden-record negatives. `npm run lint`, all edited script syntax checks, and `git diff --check` exited 0.
- Build gate: the sandboxed build first hit the known Turbopack internal-port restriction (`Operation not permitted`); the authorized non-sandboxed rerun after the final edit compiled in 5.7s, passed TypeScript in 7.8s, generated 8/8 static pages, and exited 0.
- Read-only live gate: `npm run verify:live-dashboard` exited 0. Admin RLS returned 3 cards, 2 documents, 1 project, and 28 activity rows; ordinary-member RLS returned 1 card, 1 document, 1 project, and 27 activity rows. Every returned row had a non-empty source ID, and the existing admin-only/custom hidden-record negatives passed.
- Safety boundary: the live gate performed authenticated SELECTs only. No Supabase mutation, migration, Vercel command, credential edit/output, production action, commit, or push occurred.

## Wave 2 drilldown evidence (2026-08-01)

- Node boundary: `drilldown` consumed the verified `dash` source-reference contract and seeded RLS-visible records. It did not implement the sibling global `search` node, change workflow writes, or consume decision #4.
- Nested source preservation: the app-data query now selects IDs for embedded votes, approval conditions, and project milestones. Card mapping carries exact `card`, `proposal`, `vote`, `condition`, `message`, `attachment`, and linked `document` references; document mapping carries its attachment/card/project links and citation paths; project mapping carries only its own allowance, milestones, variations, documents, invoices, and quote reviews.
- Detail surfaces: the card drawer renders a complete `Card evidence records` panel; each document exposes summary, linked records, and `Document evidence records`; each dashboard project exposes planned scope, status/progress, and `Project evidence records`. All use the shared `EvidenceReferences` component, which deduplicates but does not truncate the reference list.
- Static gates: `npm run verify:drilldown` exited 0 with `Record drilldown source-traceability verification passed.` The regression `npm run verify:dashboard`, edited-script `node --check` commands, `npm run lint`, and `git diff --check` also exited 0. The dashboard verifier was strengthened to require the new project evidence panel rather than its retired one-line label.
- Read-only live gate: `npm run verify:live-dashboard` exited 0 after asserting that the seeded admin result contains nested proposal/message/vote/condition records and every nested record has a non-empty ID. RLS counts remained admin 3 cards/2 documents/1 project/28 activity versus member 1 card/1 document/1 project/27 activity, with all hidden-record negatives still passing.
- Build gate: the final authorized `npm run build` compiled in 7.5s, passed TypeScript in 3.9s, generated 8/8 static pages, retained `ƒ Proxy (Middleware)`, and exited 0.
- Safety boundary: the live gate performed SELECTs only. No schema or RLS change, migration, Supabase write, Vercel command, credential output/edit, production action, workflow assertion weakening, commit, or push occurred.

## Wave 2 search evidence (2026-08-02)

- Node boundary: `search` consumed the verified Track C dashboard/drilldown contracts and the existing `StrataAppData` payload. It adds no server search endpoint, independent Supabase client, schema object, mailbox input, or AI search; therefore it cannot widen access beyond records already returned through the session's RLS-filtered queries.
- Search surface: `Search` is a dedicated desktop/mobile navigation destination. `SearchPage` indexes cards, documents, projects, activity, budget lines, vendors, and members; supports AND-style multi-term matching; defers the query value; memoizes both the index and filtered results; opens cards in the existing drawer and routes other results to their source collection.
- Traceability: every result renders the shared `EvidenceReferences` panel. Live cards without persisted `sourceRefs` are excluded; local seeded records receive an explicit `fallback-*` prefix. Budget mapping now preserves exact `budget_line`, `account`, `budget_allowance`, and `expense` IDs; the expense SELECT includes `id`.
- Behavior/static gate: `npm run verify:search` exited 0 with `RLS-scoped record search verification passed.` Its fixtures cover all seven record classes, complete evidence references, multi-term matching, nested-message evidence, budget/expense evidence, exclusion of an unpersisted live sentinel, a hidden-sentinel negative, and fallback labelling. It also asserts the accessible input, Search navigation/routing, RLS boundary disclosure, memoized/deferred derivation, and absence of direct client/API fetching.
- Regression/static health: `npm run verify:drilldown`, `npm run verify:dashboard`, `npm run verify:frontend-contract`, edited-script `node --check`, `npm run lint`, and `git diff --check` exited 0. The React best-practices review found no effect-derived state, redundant fetch, unstable list key, or missing accessible control name.
- Read-only live gate: the first `npm run verify:live-dashboard` run correctly revealed a valid empty vendor collection; the verifier's incorrect non-empty-vendor fixture assumption was removed while query-error and per-row-ID checks remained. The rerun exited 0. Admin/member respectively saw 3/1 cards, 2/1 documents, 1/1 projects, 37/36 activity rows, and both saw 1 budget line, 1 expense, 0 vendors, and 4 RLS-visible members; all hidden-record negatives and source-ID checks passed.
- Build gate: the final authorized `npm run build` compiled in 3.5s, passed TypeScript in 4.1s, generated 8/8 static pages, retained `ƒ Proxy (Middleware)`, and exited 0.
- Safety boundary: the live gate performed SELECTs only. No migration, Supabase write, Vercel command/deploy, credential output/edit, production action, external message, commit, or push occurred. The independent Preview upload-authorization blocker remains recorded under Waiting on operator.

## Wave 2 fe-inventory evidence (2026-08-01)

- Edge re-scope and its justification: `fe-inventory` had been recorded `blocked(decision-4)` and could never clear, because unlike decisions #7 and #8 (`GRAPH-RUN-PROMPT.md:24`) decision #4 had no mandated temporary default, and `FRONTEND-CONTRACT.md:153` forbade inferring it from the incoming code. The block was also mis-scoped: the node's contract (`GRAPH-PLAN.md:42`) is *"Inventory incoming frontend; map screens → contracts; gap list"* from a delivered frontend plus the contract doc — neither input is the #4 answer. The router at `GRAPH-PLAN.md:131` gates on frontend *arrival*; decision #4 "determines Track A's **shape**" (`GRAPH-PLAN.md:266`), which is the `fe-journey:*` integration work. The `blocked(decision-4)` edge therefore moved to the six journey nodes and `fe-inventory` ran. Decision #4 was not inferred or defaulted at that node; the operator later answered `replace` on 2026-08-01.
- Cost of the mis-scope: the delivered frontend had already been merged into `src/` while the node that audits it sat blocked. Two contract regressions were consequently caught only after the fact and recorded as retrospective fixes (the client-side keyword matcher replacing the cited AI surface, and the removal of signed-out locking and member administration). Those are exactly the defect class the gap list exists to catch before integration.
- Output: `FRONTEND-GAPS.md` records 20 gaps across six journeys plus 4 cross-cutting items, in the format mandated by `FRONTEND-CONTRACT.md:152` (screen/component, required contract, current implementation, proposed change, affected gate). Three gaps are D4-conditional and are stated with both branches, selecting neither.
- Cross-check with the concurrent `preview-1` workstream (fifth follow-up, landed while this node ran): its live `verify:ai-browser` failure is independent confirmation of this inventory's headline finding — the same missing `Budget` and `Projects` destinations. It also surfaced two gaps the static inventory had missed, now recorded: the sign-in inputs lack the `aria-label="Email"`/`"Password"` and sibling structure that `verify-ai-browser.mjs:278-302` requires (G-L1, which is why `fe-journey:login` is no longer gap-free), and the Cards AI panel (`AI panel`, `Ask AI`, `Send AI question`) is absent while the underlying `card-brief`/`thread-summary` capability already exists (G-C8).
- Critical-path consequence: decision #4 now gates `preview-1` as well as Track A. `verify:ai-browser` cannot pass under **either** branch until the Budget and Projects destinations exist, and `preview-1` is recorded `done(auth) / blocked(ai-browser on decision-4)`. This raises the cost of leaving #4 unanswered from "Track A stalls" to "the Preview fan-in stalls".
- Largest finding: four collections frozen into `StrataAppData` — `projects`, `vendors`, `budgetLines`, `budgetRecommendation` — are delivered by the backend and rendered nowhere in the composed UI. The delivered nav retired four surfaces present in the previous UI (`projects`, `budget`, `incidents`, `activity`; the dead `navItems` export at `src/lib/strata-data.ts:212-220` is the evidence) and added three (`votes`, `updates`, `settings`). This is a materially larger decision-#4 surface than the single Projects-placement item anticipated at `FRONTEND-CONTRACT.md:147`.
- Defects found during inventory, both decision-#4-neutral: `app-store.tsx:73` seeds card state once at mount and never re-syncs when `refreshData()` returns new data, so the card list is stale after any refresh (G-D1); and the adapter maps document *extraction status* into the `size` field (`building-platform-data.ts:41`), which `documents-page.tsx:58` then renders as though it were a file size (G-DOC3).
- Three change requests raised and **not** implemented, per `FRONTEND-CONTRACT.md:9`: card archive (CR-1), document download (CR-2), and building settings/notification preferences (CR-3). Each is a demo control implying persistence for which no endpoint or column exists. Dropping the control is a valid disposition.
- Inventory-time finding (subsequently resolved): the already-bound login journey and most of admin required no integration work beyond the then-Preview-blocked `role-gate`. The signed-out/uninvited/suspended lock is enforced server-side at `strata-app-data.ts:644-673` (only `status = "active"` rows resolve a member; everything else returns `mode: "signed-out"` with empty collections) rather than by UI filtering. The role gate later passed 16/16 and is recorded done above.
- Static evidence: `npm run verify:frontend-contract` exited 0 with `Frontend contract verification passed.` `npm run verify:workflow-ui` exited 1 with `Error: Missing aria-label="Card title"`, the first of 11 required accessible names; this is the expected frozen gap recorded at `FRONTEND-CONTRACT.md:110` and `:145`, not a regression, and no assertion was weakened to make it pass.
- Safety boundary: read-only node. No component, route, schema, migration, verifier, or credential was modified; no live verification script, Supabase or Vercel command, production action, commit, or push was performed. The only files written are `FRONTEND-GAPS.md`, this state file, and the scope note in `GRAPH-PLAN.md`.

## Resolutions (2026-08-01, third follow-up — MIDDLEWARE_INVOCATION_FAILED)

- Symptom: Preview `strata-532rygzcm…` returned 500 `MIDDLEWARE_INVOCATION_FAILED` on every request. Vercel runtime logs (`source: "edge-middleware"`) gave the exact error: `ReferenceError: __dirname is not defined`.
- Root cause: `middleware.ts` is the **deprecated Next 15 convention**. Next 16 renamed it to `proxy`, and per `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md`, Proxy runs on the **Node.js runtime**, while the legacy `middleware.ts` filename still deploys as an **Edge** function. Next's own compiled deps in that bundle (`cookie`, `path-to-regexp`, `p-queue`, `path-browserify`, `ua-parser-js`) reference `__dirname`, which does not exist in Edge — so it threw on every request. This was a latent Next 16 upgrade defect, unrelated to the key-resolver work.
- Fix applied (per the bundled upgrade guide, `01-app/02-guides/upgrading/version-16.md:627`): renamed `middleware.ts` → `src/proxy.ts` via `git mv` and renamed the exported function `middleware` → `proxy`. Import path adjusted to `./lib/supabase/middleware` (still relative, preserving the HANDOFF.md Vercel bundling fix). `updateSession` in `src/lib/supabase/middleware.ts` is unchanged.
- **Placement trap found and closed:** a root-level `proxy.ts` was **silently ignored** by Next (build printed no Proxy line; both middleware manifests empty; no session enforcement would have run). With a `src/app` structure the proxy must live at `src/proxy.ts`. `verify-production-ready.mjs` now asserts `src/proxy.ts` exists, so a silently undetected proxy fails the gate instead of quietly dropping session handling. The prior explicit `"middleware.ts"` entry in the secret-hygiene list was removed because `listFiles("src")` already covers the new location (and a stale filename would have been dropped by its `existsSync` filter without warning).
- Runtime evidence: build prints `ƒ Proxy (Middleware)`; `.next/server/edge` no longer exists and `sortedMiddleware` is empty (no Edge bundle); proxy builds as the Node bundle `.next/server/middleware.js`.
- Barrier re-run: **9/9 passed in one run** after the migration.
- New Preview `dpl_6RfrLFQ7nMeS4fT8ZkZed2NqDnKK` at `https://strata-gje4sirao-jjlecocq-8964s-projects.vercel.app` is READY and returns **302 to Vercel SSO** (deployment protection) with **zero runtime error logs** — the 500/ReferenceError is gone. Production remains the 34-day-old errored deployment; untouched.

## Resolutions (2026-08-01, fourth follow-up — browser verifier access)

- Browser verifiers could not run: Vercel Deployment Protection returns 302 to `vercel.com/sso` for anonymous traffic. Playwright chromium was also not installed (`npx playwright install chromium` fixed that).
- Bypass support added to `verify-auth-browser.mjs` and `verify-ai-browser.mjs` via `VERCEL_AUTOMATION_BYPASS_SECRET` (both scripts already load `.env.local`). No-op when the variable is absent, so local `127.0.0.1:3000` runs are unchanged.
- **Operator action still required:** the value currently in `.env.local` is **34 characters**; Vercel's automation bypass secret is exactly **32 alphanumeric characters**, and `curl` returns 302 both with and without the header, confirming it is not accepted. Re-copy the secret from Project Settings → Deployment Protection → Protection Bypass for Automation.
- Two defects found and fixed while diagnosing:
  - **Credential leak:** the first implementation used Playwright `extraHTTPHeaders`, which sent `x-vercel-protection-bypass` to *every* origin the page contacted (sentry.io, Google — visible as CORS preflight errors in the failing run). Replaced with the query-parameter + `x-vercel-set-bypass-cookie` form on the first navigation, so the secret goes only to the deployment origin.
  - **False-positive reachability:** `canReachApp()` followed the 302 to the Vercel login page and saw HTTP 200, so it reported the app reachable and the run failed later with a confusing assertion error. It now uses `redirect: "manual"`, detects the `vercel.com/sso` redirect, and fails immediately with remediation instructions.
- Framework-preset concern **withdrawn — not a defect.** Vercel build logs for `dpl_6RfrLFQ7nMeS4fT8ZkZed2NqDnKK` show `Running "npm run build"` → `next build` → `▲ Next.js 16.2.9 (Turbopack)` → `ƒ Proxy (Middleware)` → `Build Completed in /vercel/output`. `vercel build` auto-detects Next.js from `package.json` and emits Build Output API v3, so the "Other" preset and the `public` output directory shown by `vercel project inspect` are inert defaults, not the settings in use. Changing the preset now would alter a working build for no benefit; leave it.

## Resolutions (2026-08-01, fifth follow-up — preview serving + browser verifiers)

- **`verify:auth-browser` PASSES against Preview `https://strata-8fxconnwa-jjlecocq-8964s-projects.vercel.app`** — all 16 observations true, including `signedOutLocked`, `adminSignedIn`, `inviteCreatesPendingMember`, `backwardsInviteRejected`, `selfLockoutControlsDisabled`, `ordinaryCannotInvite`, `ordinaryCannotEdit`, `hiddenRecordsAbsent`, `suspendedLocked`, `cleanupScoped`.
- **Root cause of the 404 (framework preset — my earlier withdrawal was wrong).** `public/file.svg` returned 200 while `/` and `/api/app-data` returned 404: Vercel was serving only the `public/` directory as a static site and discarding the Next.js build. The build logs showing `next build` proved the build *ran*, not that its output was *served* — I mistook one for the other. The prior deployment's 500 masked this, because the root-level `middleware.ts` was deployed as an edge function that crashed before routing; moving it to `src/proxy.ts` removed the crash and exposed the underlying misconfiguration. Fixed in-repo with `vercel.json` (`"framework": "nextjs"`) rather than changing Vercel account settings, so the fix is version-controlled. Root, API, and static asset all return 200 since.
- Bypass secret now valid (32 chars); Vercel issues `_vercel_jwt` and redirects to `/`. Playwright chromium installed.
- UI fixes required by the frozen contract: the signed-out title rendered as a `<div>` via `CardTitle` (no heading semantics — a real accessibility regression from the v0 refactor), now a real `<h1>`; `ProjectAiTool` button label corrected to the contract's `Refresh project AI`.
- Verifier fixes (locator correctness, no assertion weakened): two strict-mode ambiguities disambiguated with `.first()` (`Supabase RLS-backed session data` now renders in both the header and the new dashboard evidence-boundary panel; a member's email renders in both its field label and its own text node). The suspended-user step reused the `signIn` helper, which waits for an authenticated header banner — self-contradictory for a user who must be locked out. Split into `submitCredentials` + the existing locked-screen assertion, which makes the check stricter, not weaker.
- **`verify:ai-browser` remains BLOCKED — this is frontend-journey work, not a defect.** The v0 refactor does not implement the AI surface the frozen contract requires: `Budget` and `Projects` navigation destinations do not exist (`sidebar-nav.tsx` has dashboard/cards/votes/updates/documents/people/settings only); the Cards `AI panel`, `Ask AI` input, and `Send AI question` control are absent; and the sign-in inputs use `<label for>` association while the verifier queries `input[aria-label="Email"]`. Building these is `fe-journey:{dashboard,projects,decisions}` work, which is edge-blocked on **decision #4** (replace vs refactor). Not attempted — doing so would expand scope into blocked nodes ahead of the router.

## Resolutions (2026-08-01, sixth follow-up — decision #4 `replace` implementation)

- Router: operator answered decision #4 with `replace`. `DECISIONS-REQUIRED.md` records the answer; decision-4 edges are removed from the six frontend journey rows without erasing their independent role/login/gap dependencies.
- New destinations: `projects` and `budget` are wired through `NavKey`, `NAV_ITEMS`, and `PAGE_META` in the delivered design language. The `people` → `Members` accessible-name mapping remains intact.
- Data coverage: Projects renders the requested financial, milestone, variation, evidence, and AI-summary fields plus `ProjectAiTool`; Budget renders every budget line, the recommendation summary/citations/disclaimer as one invariant block, all visible vendors, and `BudgetAiTool`. The dead legacy `navItems` export is removed.
- Contract gaps: G-L1 and G-C8 are implemented using the exact unchanged verifier selectors and shared AI helpers. CR-1, CR-2, and CR-3 were dropped as controls only; no endpoint, API route, migration, or schema change was introduced.
- Local evidence: the definitive 9-gate barrier passed in one captured run. Lint exited 0; Next 16 compiled, passed TypeScript, generated 8/8 pages, and retained `ƒ Proxy (Middleware)`; security, production readiness, documents, AI, NSW law, live auth, and live member-management all printed their pass results. The expected mislabeled-key fallback warning remained unchanged.

## Resolutions (2026-08-01, seventh follow-up — Preview authorization boundary)

- Attempted exactly the allowed Preview command, plain `npx vercel deploy`; no `--prod`, promote, rollback, alias, or account-setting command was used.
- The deployment approval reviewer rejected the command before process spawn because it uploads the repository/source and environment-linked build context to the linked external Vercel SaaS project. The reviewer requires a new explicit operator approval after disclosure of that external upload risk.
- No deployment was created and no Vercel or Production state changed. Under the loop contract, the node is WAITING rather than treating prior Preview proof as evidence for the new tree.
- Required continuation: operator explicitly approves uploading this repository/source and build context to its already-linked Vercel project for a Preview deployment. After approval, run the plain deploy once, then `verify:auth-browser` and `verify:ai-browser` against the same fresh URL.

## Wave 2 eve-tools evidence (2026-08-02)

- Dependency migration: the approved install completed with `eve@0.29.4`, `ai@7.0.48`, and `@ai-sdk/react@4.0.51`; `npm ls eve ai @ai-sdk/react --depth=0` reported those exact resolved versions with no peer error. `package.json` now declares Node `>=24`, matching installed Eve's documented runtime requirement; local verification ran on Node `v24.14.0`.
- Installed-version docs: implementation began only after reading `node_modules/eve/docs/README.md` and the installed project-layout, agent-config, TypeScript API, context, tools, default-harness, session-context, auth, and Next.js guides. The relevant installed Next 16 config/route docs were also read before editing `next.config.ts`.
- Runtime surface: `npx eve info` exited 0 with `Compile ready`, `Diagnostics 0 errors, 0 warnings`, `Tools 4 tools`, `Skills 0`, `Subagents 0`, and `Schedules 0`. The four tools are `search_records`, `get_card_evidence`, `get_document_evidence`, and `get_project_evidence`; nine built-ins covering shell, file access, web access, delegation, and todo state are explicitly disabled.
- Identity and scope: `agent/channels/eve.ts` accepts only a verified Supabase bearer principal with an active membership. An optional caller committee selection is accepted only when that membership query proves it. Every tool then revalidates `ctx.session.auth.current.principalId`, member ID, committee ID, and active status through the server client before reading; tool schemas expose no user/member/committee/role scope inputs. Card/document child rows are filtered to reproduce the applicable card/document visibility edges when the server client bypasses RLS.
- Output contract: every success result returns persisted source references; every invisible, absent, or unmatched lookup returns `status: "evidence_missing"`, an empty citation array, and the literal `Evidence missing:` message. The system instructions require citations on every factual claim and prohibit filling gaps, advice, or claims of writes.
- Static gates: `npm run verify:eve-tools` exited 0 and printed the four tool names, `builtInsDisabled: 9`, `scope: current principal revalidated server-side per tool call`, and `mutations: false`; the verifier rejects Supabase insert/update/upsert/delete calls, Auth-admin calls, caller-supplied scope, permissive route auth, or server credentials in public variable names. `npm run verify:security` printed `RLS and AI context verification passed.`; `node --check scripts/verify-eve-tools.mjs` and `git diff --check` exited 0.
- Compile gates: `npm run build:eve` exited 0. After generated Eve output was added to ESLint/git ignores, `npm run lint` exited 0 with no warnings. The final integrated `npm run build` (`eve build && next build`) exited 0: Eve's Nitro server built, Next compiled, TypeScript completed, 8/8 static pages generated, and `ƒ Proxy (Middleware)` remained present.
- Audit boundary: read-only `npm audit --omit=dev --json` reported four high production findings in the pre-existing Next/shadcn chain, not Eve/AI: direct `next@16.2.9` with transitive PostCSS/sharp, plus js-yaml through shadcn. No unapproved audit fix or Next upgrade was applied.
- Safety boundary: no Eve run called a model or a live tool; no Supabase row/Auth/storage mutation, deployment, external connection, schedule, message, secret output/edit, commit, or push occurred. Generated `.eve/` and `.output/` artifacts are ignored.

## Wave 2 eve-evals evidence (2026-08-02)

- Net-new coverage: `evals/` contains exactly four discovered Eve cases: `citations`, `cross-session`, `evidence-missing`, and `scope`. `STRATA_EVE_EVAL_FIXTURE=1 npx eve eval --list` printed all four descriptions and tags; the existing generic `verify:ai` was not reused as evidence.
- Production boundary: the deterministic model/data fixture activates only when `STRATA_EVE_EVAL_FIXTURE=1` and `NODE_ENV !== "production"`; route authentication additionally requires Eve's `isLoopbackRequest(request)` check. Without those conditions, the pre-existing Supabase bearer authenticator and per-tool active-member revalidation remain authoritative. The fixture contains only in-memory read data and the static verifier rejects insert/update/upsert/delete calls.
- Scope and isolation: separate admin and ordinary-member Eve sessions queried the same hidden sentinel record. The admin received it; the ordinary member received literal `Evidence missing:` and no sentinel. The cross-session case also asserted distinct durable session IDs and repeated the hidden-card attempt with adversarial client context.
- Citation and fallback: the visible-card case called `get_card_evidence` with the fixture UUID and required the persisted `card:<uuid>` citation and title. The unknown-card case called the same real tool surface and required `Evidence missing:` plus the requested UUID, preventing an uncited fabricated answer.
- Definitive runner gate: `npm run verify:eve-evals` exited 0 against a real loopback Eve HTTP target and printed `Results: 4 passed (4 total)` and `Gates: 17 passed`; per-case totals were citations 3/3, cross-session 6/6, evidence-missing 3/3, and scope 5/5. A post-summary local queue teardown retry logged `socket hang up` after the target closed, but the runner completed successfully with exit 0.
- Regression gates: `npm run verify:eve-tools` exited 0 with four tools, nine built-ins disabled, and `mutations: false`; `npm run lint`, `npm run verify:security`, and `git diff --check` exited 0. The final production-mode `npm run build` exited 0 after Eve/Nitro compilation, Next compilation, TypeScript, 8/8 static pages, and `ƒ Proxy (Middleware)`.
- Safety boundary: no live Supabase/model-provider request, database/Auth/storage mutation, external message, deployment, secret output/edit, commit, or push occurred. The only elevated operations opened local loopback/worker ports needed by the Eve runner and Turbopack.

## Wave 2 eve-drafts evidence (2026-08-02)

- Router result: decision #3 assigns sole approval to the repository operator/owner until the policy is explicitly fine-tuned. `DECISIONS-REQUIRED.md` records the answer and date; no committee or ordinary-member approval path was added.
- Write boundary: the only new mutations are `save_proposal_draft` and `save_approval_condition_draft`. Both persist `status = draft` rows and matching audit evidence; neither can publish, send, schedule, activate, approve, update official state, or delete data. Stable IDs derived from Eve's call ID make resumed/replayed execution an idempotent upsert rather than a duplicate write.
- Authorization boundary: production identifies the sole approver through server-only `STRATA_EVE_APPROVER_USER_ID`. The approval policy first requires an active authenticated membership and exact operator UUID match; after the human prompt resumes, the executor independently revalidates the current active session and exact operator match before any write. Missing/invalid configuration and all non-operator sessions fail closed.
- Evidence boundary: both executors recheck the target card's visible committee scope; the condition executor also verifies that the referenced proposal belongs to that card and committee. Successful output requires `status: draft_saved`, `persisted: true`, and citations for both the draft and audit record.
- Deterministic HITL gate: `npm run verify:eve-evals` exited 0 against the loopback fixture with `Results: 8 passed (8 total)` and `Gates: 40 passed`. Approval cases parked before persistence and completed only after an admin-principal approval response; human denial and ordinary-member attempts persisted nothing. The post-result queue shutdown warning occurred after the successful exit and did not affect the gate result.
- Static/compile gates: `npm run verify:eve-tools`, `npm run lint`, `npm run verify:security`, and `git diff --check` exited 0. The final authorized `npm run build` exited 0 after Eve/Nitro compilation, Next.js compilation, TypeScript, 8/8 static pages, and `ƒ Proxy (Middleware)`.
- Activation boundary: no credential value was read, printed, or edited. The implementation is complete but remains fail-closed at runtime until the operator securely sets `STRATA_EVE_APPROVER_USER_ID` in local and Preview server environments to the Supabase Auth UUID of the account that should own approval.
- Safety boundary: fixture evals used in-memory data only. No live Supabase/model call, migration, deployment, message, production mutation, secret output, commit, or push occurred.

## Wave 2 fe-qa evidence (2026-08-03)

- Fresh-eyes router: an independent read-only reviewer first returned FAIL, identifying false People vote/unit claims, missing mobile sign-out, a hand-rolled assistant dialog without modal focus lifecycle, unreachable short-viewport dialogs, undersized targets, insufficient success-text contrast, missing skip/focus navigation, and a no-op Notifications control. After remediation, a second review caught short-landscape mobile-menu reachability, modal-close focus restoration, and incomplete composed target coverage. The final independent review returned **PASS — no remaining `fe-qa` blockers**.
- Truthful data: the live member adapter no longer derives a unit or vote status from access/status. `Person.contextLabel` renders the persisted access context, People reports only the workspace-member count, and record search uses the same truthful label. No RLS, workflow, voting, or backend assertion was weakened.
- Accessibility and responsive behavior: mobile navigation now scrolls independently with a fixed reachable footer/sign-out; page-heading focus waits for the modal sheet close transition; the assistant uses Base UI Sheet for focus trapping, Escape, and focus restoration; shared dialogs cap at `100dvh - 2rem` and scroll; the skip link and post-navigation heading focus are explicit; the no-op Notifications control was removed; and the People mail action renders with anchor semantics.
- Target/contrast evidence: Buttons, Inputs, InputGroups, Tabs, Selects, Toggles, native workflow selects, assistant chips, AI input, source buttons, and interactive summaries use at least 44px targets on mobile. The light success token changed to `oklch(0.44 0.12 155)`; the captured OKLCH-to-relative-luminance calculation returned a 6.93:1 contrast ratio against the light background (above 4.5:1), with a separate lighter dark-theme token.
- Repeatable browser gate: `npm run verify:frontend-qa-browser` exited 0 with all 13 observations true: signed-out desktop, admin desktop navigation, assistant keyboard lifecycle, document dialog, mobile navigation/sign-out/dialogs, landscape and 200%-zoom-equivalent reflow, mobile targets, ordinary-member restrictions, no horizontal overflow, no framework overlay, and no console errors. It signs into the pre-existing active admin/member accounts, submits no workflow/document/member-management/AI form, and checks both roles.
- Static/compile gates: `node --check scripts/verify-frontend-qa-browser.mjs`, `npm run verify:frontend-contract`, `npm run verify:search`, `npm run lint`, and `git diff --check` all exited 0. The final authorized `npm run build` exited 0 after Eve/Nitro build, Next compilation, TypeScript, 9/9 static pages, and `ƒ Proxy (Middleware)`.
- Safety boundary: no migration, deployment, production action, workflow/document/member-management/AI submission, external message, credential value edit/output, commit, or push occurred. The existing mislabeled local `SUPABASE_SECRET_KEY` warning and fallback were not changed.

## Wave 3 preview-n hard-stop evidence (2026-08-03)

- Preview artifact: read-only `vercel inspect` returned deployment ID `dpl_Dkaot5hqJd6u66QbX4uadQLDKz5S`, URL `strata-odxmhg7ur-jjlecocq-8964s-projects.vercel.app`, `target: "preview"`, `readyState: "READY"`, framework `nextjs`, and Node `24.x`.
- Approver environment boundary: read-only `vercel env ls preview --format json` returned `STRATA_EVE_APPROVER_USER_ID` with `type: "sensitive"` and `target: ["preview"]`. No environment value was requested or printed.
- Production-isolation failure: read-only `vercel list strata-ai --environment production --format json` returned a new deployment `strata-foydsu43o-jjlecocq-8964s-projects.vercel.app`, `target: "production"`, `state: "ERROR"`, `createdAt: 1785747178427`, metadata `actor: "codex"`, `action: "redeploy"`, `originalDeploymentId: "dpl_Ec9vCo1o3hVW9n5mqC1pFLCArbQF"`. The previously recorded Production deployment `strata-girsmzdpo-jjlecocq-8964s-projects.vercel.app` remains listed separately, so Production state was not preserved.
- Router/stop: `preview-n` cannot claim the strict “Production untouched” criterion. Per full-story verification, execution stopped at this first broken boundary. No `verify:auth-browser`, workflow browser, AI browser, promotion, rollback, alias change, deletion, deployment, environment edit, migration, commit, or push was performed by this diagnostic.
- Operator disposition: the operator stated that they clicked Deploy in the Vercel dashboard after entering the approver user ID, then explicitly approved continuing with `verify:auth-browser`. This accepts the already-recorded Production-isolation failure as a variance for `preview-n`; it does not erase the incident or authorize any Production mutation.
- Role-gate evidence: `STRATA_BROWSER_URL=https://strata-odxmhg7ur-jjlecocq-8964s-projects.vercel.app npm run verify:auth-browser` exited 0 with `ok: true`. All 16 observations were true: `signedOutLocked`, `adminSignedIn`, `inviteFormEnabled`, `inviteCreatesPendingMember`, `loadingState`, `successState`, `activeState`, `backwardsInviteRejected`, `inactiveState`, `selfLockoutControlsDisabled`, `ordinarySignedIn`, `ordinaryCannotInvite`, `ordinaryCannotEdit`, `hiddenRecordsAbsent`, `suspendedLocked`, and `cleanupScoped`.
- Live-mutation boundary: the operator explicitly approved this single Preview auth-browser run, including its marked Supabase test-user/member mutations and cleanup. The only emitted credential-related text was the pre-existing resolver warning that the local `SUPABASE_SECRET_KEY` is not a server credential and that the script fell back to `SUPABASE_SERVICE_ROLE_KEY`; no value, prefix, or length was printed. No Vercel command, deployment, Production action, migration, commit, or push was performed.

## Wave 3 rehearsal evidence (2026-08-03)

- Rehearsal scope router: signed decision #7 excludes Track B/meeting-mode from v1 and signed decision #8 defers Track E post-v1. The in-scope fan-in is the Ready `preview-n`, base role gate, consolidated frontend QA, and Eve evals; no mailbox, intake, meeting-mode, or Track E work was pulled into the barrier.
- Eve gate: the first sandboxed invocation passed its source verifier but could not bind its loopback fixture (`listen EPERM: operation not permitted 127.0.0.1`). The same local-only fixture was rerun outside the sandbox and exited 0 with `Results: 8 passed (8 total)` and `Gates: 40 passed`; citations, cross-session isolation, both draft approvals, denial, unauthorized access, evidence-missing fallback, and scope all passed. The runner recorded `liveSupabaseCalls: false`. A queue retry logged HTTP 503/socket hang-up only after the successful result and exit.
- Deployed UI attempt 1: `verify:frontend-qa-browser` timed out before the sign-in heading because that verifier lacked the Vercel protection-bypass cookie path already present in the auth and AI verifiers. The verifier was hardened to use `VERCEL_AUTOMATION_BYPASS_SECRET` only on first navigation, no-op when absent. No UI assertion was changed; `node --check scripts/verify-frontend-qa-browser.mjs` and `npm run lint` exited 0.
- Deployed UI attempt 2: against `https://strata-odxmhg7ur-jjlecocq-8964s-projects.vercel.app`, 12 observations were true: signed-out desktop, admin desktop navigation, assistant keyboard lifecycle, admin document dialog, admin mobile navigation/sign-out/dialogs, landscape/zoom reflow, mobile targets, ordinary-member restrictions, no horizontal overflow, and no framework overlay. `noConsoleErrors` was false because the browser emitted `Failed to load resource: the server responded with a status of 404 ()` twice.
- Broken boundary and hard stop: the current verifier captures console text but not the response URL, and the static audit found `src/app/favicon.ico` plus no obvious missing local asset reference. After two attempts of the deployed UI step, no third retry was made. The next diagnostic must capture failed-response URL/status (without credentials), fix the identified resource/route, and then obtain a fresh operator-authorized rehearsal run.
- Safety boundary: the UI rehearsal signed into the existing admin/member test accounts but submitted no workflow, document, member-management, or AI form. No live workflow rows, AI disclosure, migration, Vercel mutation/deployment, Production action, credential value output/edit, commit, or push occurred.

### Rehearsal 404 remediation and fresh Preview

- Authorized diagnostic: `verify-frontend-qa-browser.mjs` now records failed-response method, resource type, status, and a query-stripped location. The diagnostic run reproduced the two console errors and identified both exactly as `GET`, resource type `fetch`, status `404`, location `/recover`; no query string, header, body, cookie, bypass secret, or credential was logged.
- Root cause and fix: Vercel inspection showed the old artifact already contained a `recover` function, and the local Next build emitted `ƒ /recover`, disproving route-file absence. The failures were automatic production-only App Router prefetches from the signed-out `Forgot password?` link. Following the installed Next.js 16 Link guidance, the link now uses `prefetch={false}`; click navigation and all recovery logic remain unchanged.
- Local proof: `node --check scripts/verify-frontend-qa-browser.mjs`, `npm run verify:recovery`, `npm run verify:frontend-contract`, and `npm run lint` exited 0. The post-fix `npm run build` compiled, passed TypeScript, generated 9/9 static pages, retained `ƒ /recover`, and retained `ƒ Proxy (Middleware)`.
- Fresh artifact: plain `vercel deploy --yes` created Preview `https://strata-5z8ywdcrp-jjlecocq-8964s-projects.vercel.app`; read-only inspection returned deployment ID `dpl_HQ7AD2sLp5tij1dyryzYPsJiiVFr`, `target: preview`, `status: Ready`, and a `recover` function. No `--prod`, promote, rollback, or alias command was used.
- Final deployed UI gate: `STRATA_BROWSER_URL=https://strata-5z8ywdcrp-jjlecocq-8964s-projects.vercel.app npm run verify:frontend-qa-browser` exited 0 with `ok: true`. All 13 observations were true, including both roles, responsive/dialog/focus/target checks, no overflow, no framework overlay, and `noConsoleErrors`; the new diagnostic fields were exactly `consoleErrors: []` and `failedResponses: []`.
- Production isolation: the read-only before/after Production listings were identical: only the pre-existing errored deployments `strata-foydsu43o-jjlecocq-8964s-projects.vercel.app` and `strata-girsmzdpo-jjlecocq-8964s-projects.vercel.app` appeared, with unchanged timestamps and metadata. The fresh artifact was Preview-only.
- Human gate disposition: after the objective rehearsal evidence was clean, the repository operator instructed the graph to move to the next phase on 2026-08-03. This closes `rehearsal` and authorizes preparation of the read-only go/no-go report; it does not authorize Production. No live workflow row, recovery mutation, AI disclosure, migration, Production action, credential edit/output, commit, or push occurred.

## Go/no-go evidence (2026-08-03)

- Output: `GO-NO-GO.md` was created with evidence, gaps, recovery, next scope, and untouched operator checkboxes. Recommendation: **NO-GO / HOLD**; the operator's final decision remains pending.
- Initial security blocker: refreshed `npm audit --omit=dev --json` exited 1 with `high: 4`, `critical: 0`. Direct `next@16.2.9` was inside the affected `<16.2.11` range for the App Router Middleware/Proxy bypass, directly relevant to Strata's `src/proxy.ts` session boundary. This finding is preserved as discovery history and was subsequently resolved by the dependency-hardening node below.
- Release-revision blocker: `git status --short` returned a broad dirty tree with tracked and untracked Wave 1–3 work. The tracked diff alone reports 43 files, 5,079 insertions, and 2,948 deletions. No commit or push is authorized or present for this release candidate.
- Router status at report creation: decisions #5–#8 were initially pending. The operator subsequently signed verified fallback, the current environment names, no meeting-mode in v1, and Track E deferred post-v1 on 2026-08-04. Deferred #1/#2/#9/#10 remain outside the signed v1 scope.
- Recovery blocker: the Production list contains only the two pre-existing errored deployments and no captured Ready rollback target. The final rehearsal's before/after lists were identical, but a production recovery procedure cannot yet name a known-good Production artifact.
- Safety boundary: the go/no-go node performed read-only audit/status inspection and wrote only repository documentation/state. No dependency was changed, no secret was printed or edited, and no Vercel/Supabase/Production mutation, commit, or push occurred.

## Dependency-hardening evidence (2026-08-04)

- Resolution path: `next` first moved from 16.2.9 to the originally recommended 16.2.12, clearing the Proxy-bypass advisory. The refreshed registry audit then identified newer PostCSS/sharp advisories with stable `next@16.3.0` as the supported fix. Registry metadata confirmed 16.3.0 as `latest` with compatible React and Node requirements; no codemod or application API migration applied.
- Final versions: `next@16.3.0`, `eslint-config-next@16.3.0`, `tailwindcss@4.3.3`, `@tailwindcss/postcss@4.3.3`, Next PostCSS 8.5.23, shared PostCSS 8.5.25, `sharp@0.35.3`, `js-yaml@4.3.1`, and `hono@4.13.0`. React/React DOM remained 19.2.4. No override or `npm audit fix --force` was used.
- Audit result: final `npm audit --omit=dev --json` exited 0 with `total: 0`, `high: 0`, `critical: 0` across 742 dependencies.
- Hardened barrier result: all nine gates passed in strict order — lint; integrated Eve + Next 16.3 build (TypeScript, 9/9 static pages, `ƒ /recover`, `ƒ Proxy (Middleware)`); security; production readiness (`storageBucket: true`, `memberRlsReads: true`); documents; AI; NSW law; live Auth; and live member management.
- Live boundary: the operator's approval covered the complete barrier. Document/storage, Auth, and member-management verifiers used their scoped cleanup paths and all exited 0. The known mislabeled local `SUPABASE_SECRET_KEY` warning appeared and the resolver safely fell back to `SUPABASE_SERVICE_ROLE_KEY`; no key value was printed.
- Safety boundary: dependency and lockfile changes plus local/build artifacts only. No Preview or Production deployment, Vercel mutation, schema migration, external message, credential edit/output, commit, or push occurred.

## Release-decisions evidence (2026-08-04)

- Decision #6 name-only read-back: `vercel env ls preview --format json` returned Preview-scoped `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`, and `SUPABASE_SERVICE_ROLE_KEY`; both server-key entries were `sensitive`. `STRATA_EVE_APPROVER_USER_ID` was also sensitive/Preview-only. The command returned metadata only and no values.
- Supabase boundary: browser/server session clients read `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` with temporary `NEXT_PUBLIC_SUPABASE_ANON_KEY` fallback. The admin client imports `server-only`, validates `SUPABASE_SECRET_KEY`, and temporarily falls back to a valid `SUPABASE_SERVICE_ROLE_KEY`. No elevated credential is read by a browser client or a `NEXT_PUBLIC_*` variable.
- Decision #5 limitation: `verify-ai-browser.mjs` sets a verification marker, and `runAiTask()` adds `forceFallback: "true"`; its passing Preview evidence therefore proves the persisted, RLS-filtered fallback path only. The deployed route will otherwise attempt `openai/gpt-5.4` whenever Vercel supplies OIDC, and then returns a sanitized `error-fallback` if generation fails. No unforced live-AI smoke test has been captured.
- Current platform check: Vercel documents that deployed applications receive `VERCEL_OIDC_TOKEN` automatically, so absence from the user-managed Preview variable listing does not mean live Gateway is disabled. Supabase's current key guidance confirms publishable keys are suitable for clients and secret/service-role keys must remain backend-only because they bypass RLS.
- Signed v1 router package: #5 `verified fallback mode`; #6 `yes`; #7 `no`; #8 `defer-track-e-post-v1`. The operator approved the complete package on 2026-08-04. Mailbox/meeting-mode and Track E stay deferred until their outstanding policy routers are answered.
- Enforcement: the AI route treats only exact `STRATA_AI_RELEASE_MODE=live` as permission to call Gateway; absent, invalid, or `fallback` values use the verified persisted mock path even when Vercel supplies OIDC. The static AI verifier requires both the fail-closed router and the documented fallback default.
- Safety boundary: source/documentation changes and static/local verification only. No AI request, credential value read/output/edit, environment mutation, deployment, Supabase mutation, commit, push, or Production action occurred.

## Lessons

- Preserve pre-existing UI/configuration changes and the relative middleware import during consolidation.
- Never print environment values; inspect only variable names/presence.
- Existing generic `verify:ai` is not evidence for the net-new `eve-evals` contract.
- Deterministic agent evals can exercise Eve's real HTTP, auth, tool, and durable-session surfaces without live data or provider spend when fixture mode is guarded by explicit env, non-production, and loopback checks.
- A server-only compatibility fallback must be exempted narrowly from browser-facing secret scans; `verify:production-ready` now requires the `server-only` guard before excluding only `src/lib/supabase/admin.ts`.
- The frontend refactor had removed signed-out locking and member administration. Those contracts now live in `strata-app.tsx`, `app-shell.tsx`, and `pages/people-page.tsx`; verifiers inspect the composed client surface instead of the retired monolith.
- Live verifier cleanup must run on failure paths. Readiness, document, and AI checks now use `finally`; document cleanup attempts all resource classes before reporting aggregate failure.
- Current Supabase documentation confirms publishable keys belong in public clients, secret keys remain backend-only and bypass RLS, and legacy keys may coexist during staged migration; the consolidation follows that model.
- A human-decision edge belongs on the node whose *contract* consumes the answer, not on every node downstream of the topic. `fe-inventory` sat permanently blocked on decision #4 while its own inputs — a delivered frontend and the frozen contract — were both present, and the work it audits proceeded un-inventoried. When a router has no mandated default, check that each blocked node genuinely needs the answer before it is edged.
- A read-only analysis node is the cheapest place to catch an integration defect, so it should run *early* and unblocked. Analysis that produces conditional output for both branches of an unresolved decision is strictly more useful than analysis deferred until the decision lands.

## Wave scores

- Wave 1 decisions: 8/10 — v1 launch routers #3–#8 are explicit; deferred Track B/Track E policy decisions #1–#2 and #9–#10 remain pending before those tracks can resume.
- Wave 1 consolidation: 10/10 — output contract met with static evidence and the migrated tree subsequently passed the full 9-gate barrier.
- Wave 1 local gate: 9/10 — barrier passed 9/9 in one run (2026-08-01, second follow-up); the remaining point is the mislabeled `SUPABASE_SECRET_KEY` value in `.env.local`, which the operator must replace so the resolver fallback warning clears.
- Wave 1 preview-1: 10/10 — link/env-name/server-key boundaries were established; the final Preview passed recovery 6/6, auth/role 16/16, and AI browser 10/10 with zero marked `ai_outputs` residue. Earlier protection/share-link/deployment defects are retained below as resolved audit history.
- Wave 2: 10/10 — `auth-harden`, recovery round trip, role gate, all six frontend journeys, `fe-freeze`, fresh-eyes `fe-qa`, Track C `dash → {drilldown, search}`, `fe-inventory`, and Track D `eve-tools → eve-evals → eve-drafts` are implemented with evidence; the operator UUID is configured sensitive/Preview-only.
- Wave 3: 9/10 — in-scope fan-in, Eve 40/40, role gate 16/16, clean Preview UI rehearsal 13/13, zero-vulnerability dependency hardening, hardened 9/9 barrier, and the go/no-go document are complete. Production remains held on a traceable release revision, explicit launch routers, exact-candidate Preview/browser proof, recovery planning, and final operator decision.

## Waiting on operator

- Replace the `SUPABASE_SECRET_KEY` value in `.env.local` with the project's real secret key (`sb_secret_…`) from the Supabase dashboard, or remove the mislabeled entry. The resolver currently falls back to `SUPABASE_SERVICE_ROLE_KEY` with a warning; the barrier passes either way.
- Complete decisions #1–#2 and #9–#10 before resuming the deferred Track B or Track E work.
- Separately authorize the `release-preview` node: configure the non-secret Preview-only `STRATA_AI_RELEASE_MODE=fallback` setting if absent, deploy the published candidate branch to Preview only, verify Ready/Production isolation, and run the candidate browser barrier under its existing mutation/disclosure approvals. Production remains a separate hard stop.
